# SHIP STAGING — p20-saju-miniapp (neo y/n)

```
legion-beacon.js   # anon+anonId pair (C4)
script.js          # peak/pipe path already live (if dirty, include)
```

```bash
cd ~/p20-saju-miniapp && git add legion-beacon.js script.js && git status
```

Smoke: generate→doReading → sharePeak+moneyPipe 5/5 · `./verify.sh` PASS  
App: **saju-miniapp**
